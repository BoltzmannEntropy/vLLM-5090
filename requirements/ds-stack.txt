# Data Science Stack for Training and Inference Scripts
# Required for scripts/train.py, scripts/infer.py, and app-*.py

# Core Data Science Libraries
pandas >= 1.5.0
scipy >= 1.10.0
scikit-learn >= 1.2.0

# Deep Learning and Training
peft >= 0.7.0  # Parameter-Efficient Fine-Tuning (LoRA)
trl >= 0.7.0   # Transformer Reinforcement Learning (SFTTrainer)
datasets >= 2.14.0  # Hugging Face Datasets
accelerate >= 0.25.0  # Distributed training
bitsandbytes >= 0.41.0  # 8-bit optimizers

# Inference Utilities
logits-processor-zoo >= 0.1.0  # Logits processors for vLLM

# Text Processing
cleantext >= 1.1.0  # Text cleaning utilities
sentence-transformers >= 2.2.0  # Sentence embeddings

# Web UI and Interactive Apps
gradio >= 4.0.0  # Web UI for demos and apps
pillow >= 9.0.0  # Image processing (PIL)
opencv-python >= 4.8.0  # Computer vision (cv2)
requests >= 2.28.0  # HTTP requests

# Jupyter and Notebooks
jupyter >= 1.0.0
jupyterlab >= 4.0.0
notebook >= 7.0.0
ipywidgets >= 8.0.0

# Progress Bars and Visualization
tqdm >= 4.65.0
matplotlib >= 3.7.0
seaborn >= 0.12.0

# Utilities
python-dotenv >= 1.0.0  # Environment variables
